package src.classes;

public class Source extends Noeud {
  public Source(Reseau reseau) {
    super("source", 0, 0);

    // reseau.ajouterNoeud(this);

    // for (Noeud noeud : reseau.getNoeuds()) {
    // new Arc(this, neoud, noeud.getA())
    // reseau.ajouterArc(arc);
    // }

    // TODO: Faire pareil pour le puits
  }
}
